% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tip.R
\name{tip}
\alias{tip}
\title{Add a tooltip to a Shiny element or text}
\usage{
tip(
  tag,
  content = NULL,
  content_disabled = NULL,
  position = getOption("shinytip.position", "top"),
  width = getOption("shinytip.width", "line"),
  theme = tip_theme(),
  ...
)
}
\arguments{
\item{tag}{A Shiny tag, tagList, or plain text to add a tooltip to.}

\item{content}{The text in the tooltip. Can include emojis, but cannot contain HTML.
Use \verb{\\n} to force a new line. Can be \code{NULL} if \code{content_disabled} is given, in which case
tooltip is only shown while the input is disabled.}

\item{content_disabled}{The text to show in the tooltip while the input is disabled. See the
\emph{Disabled inputs} section.}

\item{position}{The position of the tooltip in relation to the tag. One of: \code{"top"}, \code{"bottom"},
\code{"left"}, \code{"right"}, \code{"top-left"}, \code{"top-right"}, \code{"bottom-left"}, \code{"bottom-right"}.}

\item{width}{How wide should the tooltip be? One of: \code{"line"} (place the entire tooltip in one
line), \code{"fit"} (the tooltip should have the same width as the tag), \code{"s"} (small), \code{"m"} (medium),
\code{"l"} (large), \code{"xl"} (extra large).}

\item{theme}{A \code{\link[=tip_theme]{tip_theme()}} object holding the tooltip's appearance (colours, font size,
animation, cursor).}

\item{...}{Additional attributes to pass to the tag.}
}
\value{
A Shiny tag that supports tooltips.
}
\description{
Tooltips can be added to any Shiny UI elements such as tags, inputs, outputs, or plain text.
Tooltips are powered by the project \code{balloon.css}.\cr\cr
All the theme parameters, along with \code{position} and \code{width}, can be set globally in order to use
a default setting for all tooltips in your Shiny app. This can be done by setting an R option with
the parameter's name prepended by \code{"shinytip."}. For example, to set all tooltips to appear on the
right and have a red background, use \code{options(shinytip.position = "right", shinytip.bg = "red")}.
Only \code{tag}, \code{content}, and \code{content_disabled} cannot be set globally.
}
\details{
Note that when adding a tooltip to an \verb{<img>} tag or an icon (such as fontawesome),
the tag will get wrapped in a \verb{<div>}. When adding a tooltip to plain text, the text is wrapped
in a \verb{<span>}. In all other cases, no additional HTML tags are created.
}
\section{Disabled inputs}{

Use \code{content_disabled} to give an input a tooltip that appears when it's disabled,
which can be used for explaining \emph{why} an input is disabled.\cr\cr
Providing only \code{content_disabled} without \code{content} results in a tooltip that is shown
only while the input is disabled. Providing both \code{content} and \code{content_disabled}
results in a tooltip that swaps its text depending on the input's state.\cr\cr
Any inputs disabled with \code{shinyjs::disable()} are automatically detected.
}

\section{Limitations}{

\itemize{
\item The best position for the tooltip cannot be detected automatically.
This means that you may need to adjust the position of the tooltip depending on where it appears
on the page.
\item The balloon project makes use of pseudo-elements, so if you're trying to
add a tooltip to an element that already has pseudo-elements, it may not work.
\item On mobile (and other touch devices), all tooltips are only shown on click, since hovering
is not a supported interaction.
\item If an element loses its opacity when disabled, then the tooltip will also lose its
opacity when the element is disabled. This commonly affects tooltips on disabled \code{actionButton()}.
}
}

\examples{
if (interactive()) {
  library(shiny)
  library(shinytip)

  shinyApp(
    ui = fluidPage(
      tip("hover over me", "a tooltip", position = "right"), br(), br(), br(),
      tip(actionButton("btn", "hover me"), "Hello"),
      tip(
        actionButton("btn3", "can't touch this", disabled = TRUE),
        content_disabled = "You do not have permission to do this"
      )
    ),
    server = function(input, output) {}
  )
}

}
\seealso{
\code{\link[=tip_input]{tip_input()}}, \code{\link[=tip_icon]{tip_icon()}}, \code{\link[=tip_theme]{tip_theme()}}
}
