% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tip_theme.R
\name{tip_theme}
\alias{tip_theme}
\title{Create a reusable tooltip theme}
\usage{
tip_theme(
  bg = getOption("shinytip.bg", "black"),
  fg = getOption("shinytip.fg", "white"),
  fontsize = getOption("shinytip.fontsize", "12px"),
  radius = getOption("shinytip.radius", "2px"),
  animate = getOption("shinytip.animate", TRUE),
  move = getOption("shinytip.move", "4px"),
  pointer = getOption("shinytip.pointer", TRUE)
)
}
\arguments{
\item{bg}{Background colour of the tooltip.}

\item{fg}{Colour ("foreground") of the tooltip text.}

\item{fontsize}{The font size of the tooltip text. A number is interpreted as pixels.}

\item{radius}{The roundness of the tooltip's corners. A number is interpreted as pixels.}

\item{animate}{If \code{TRUE}, animate the tooltip appearing and disappearing.}

\item{move}{How far the tooltip slides while animating into view. A number is interpreted as
pixels. Has no effect when \code{animate} is \code{FALSE}.}

\item{pointer}{If \code{TRUE}, change the cursor when hovering over the tag.}
}
\value{
A \code{shinytip_theme} object, to be passed to the \code{theme} argument of \code{\link[=tip]{tip()}},
\code{\link[=tip_icon]{tip_icon()}}, or \code{\link[=tip_input]{tip_input()}}.
}
\description{
Bundles the appearance options shared by \code{\link[=tip]{tip()}}, \code{\link[=tip_icon]{tip_icon()}} and \code{\link[=tip_input]{tip_input()}} into a single
object that can be defined once and reused. The result  can be passed to their \code{theme} argument.\cr\cr
All theme parameters can be set globally in order to use a default setting for all tooltips in your
Shiny app. This can be done by setting an R option with the parameter's name prepended by \code{"shinytip."}.
For example, to set all tooltips to have a red background and font size 24, use
\code{options(shinytip.bg = "red", shinytip.fontsize = 24)}.
}
\examples{
warning_theme <- tip_theme(bg = "#B00020", fg = "white", fontsize = 14)

if (interactive()) {
  library(shiny)
  library(shinytip)

  shinyApp(
    ui = fluidPage(
      tip("hover over me", "a red tooltip", position = "right", theme = warning_theme),
      tip("and me", "the same red tooltip", position = "bottom", theme = warning_theme)
    ),
    server = function(input, output) {}
  )
}

}
\seealso{
\code{\link[=tip]{tip()}}, \code{\link[=tip_icon]{tip_icon()}}, \code{\link[=tip_input]{tip_input()}}
}
